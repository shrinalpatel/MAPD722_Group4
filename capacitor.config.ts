import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'MAPD722_M2_Group4',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
